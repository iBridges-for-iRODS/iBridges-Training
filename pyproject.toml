[project]
name = "iBridges Training"
version = "0.1.0"
description = "Training material for the iBridges Suite: GUI, CLI, API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.1",
    "numpy>=2.2.5",
]
